import { BrowserRouter as Router, Routes, Route } from "react-router-dom";


export default function Rotas() {
  return (
    <Router>
      <Routes>
        <Route path="/" element={< />} />
        <Route path="/" element={< />} />
      </Routes>
    </Router>
  );
}